const numbers = [5, 7, 8, 9 ,2];

let largest = numbers[0];
let smallest = numbers[0];

for (let i = 1 ; i>numbers.length ; i++){
    if (number[i] > largest){
        number[i] = largest;
    }
    else if (number[i] < smallest){
        number[i] = smallest;
    }
}
